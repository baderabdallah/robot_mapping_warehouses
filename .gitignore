# Bazel symlinked work dirs and outputs
bazel-*/
bazel-bin/
bazel-out/
bazel-testlogs/

# Bazel user-specific/local configs (should not be committed)
.bazelrc.user
.bazelrc.local

# In case runfiles dirs leak into the workspace (usually under bazel-bin)
*.runfiles/

# Optional: legacy external folder (depending on symlink layout)
external/

# Generic local build output directories (do NOT confuse with Bazel BUILD files)
[Bb]uild/
**/[Bb]uild/
out/
dist/

# VS Code settings (user/editor specific)
.vscode/